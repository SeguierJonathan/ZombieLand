<% if(locals.notifications?.length){%>
    <div id="notifications">
        <% notifications.forEach(notification=> { %>

            <div class="notification <%= notification.type%>">
                <p>
                    <%=notification.message%>
                </p>
            </div>

            <% }); %>
    </div>
    <% } %>