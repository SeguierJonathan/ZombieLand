const modal = {
    open: (id) => {
        const dialog = document.getElementById(id);
        if (dialog) dialog.showModal();
    },
    close: (id) => {
        const dialog = document.getElementById(id);
        if (dialog) dialog.showModal();
    }
}